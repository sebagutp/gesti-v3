// Tests reubicados a src/lib/calculos/__tests__/liquidacion.test.ts
// Este archivo queda vacío para evitar conflictos.
import { describe, it, expect } from 'vitest'

describe('liquidacion (placeholder)', () => {
  it('tests en src/lib/calculos/__tests__/liquidacion.test.ts', () => {
    expect(true).toBe(true)
  })
})
