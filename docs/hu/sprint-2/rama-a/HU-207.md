# HU-207 — checkPlanAccess Real + Billing Plan API
**Sprint:** 2 | **Rama:** A | **Media**

## Definición
Convertir `checkPlanAccess` de stub a implementación real.
Actualizar `GET /api/billing/plan` para eliminar referencias a Stripe.

## checkPlanAccess
```typescript
export async function checkPlanAccess(
  userId: string,
  feature: Feature
): Promise<{ allowed: boolean; reason?: string }> {
  // 1. Consultar user_billing por user_id
  // 2. Si no existe → plan Free
  // 3. Si plan_status !== 'active' → no permitido
  // 4. Si end_date < now → marcar expired, no permitido
  // 5. Verificar feature en PLAN_FEATURES[plan_type]
  // 6. Retornar { allowed: true/false, reason }
}
```

### Features por plan
```typescript
const PLAN_FEATURES = {
  Free: ['simular_contrato', 'simular_liquidacion'],
  Pro_Mensual: [
    'simular_contrato', 'simular_liquidacion',
    'contrato_valido', 'liquidaciones', 'pdf_generacion',
    'multi_contrato', 'soporte'
  ],
  Pro_Anual: [
    'simular_contrato', 'simular_liquidacion',
    'contrato_valido', 'liquidaciones', 'pdf_generacion',
    'multi_contrato', 'soporte', 'whatsapp_bot'
  ],
}
```

## GET /api/billing/plan
- Eliminar referencia a `stripe_subscription_id`
- Retornar `has_active_plan` booleano basado en `plan_status === 'active' && end_date > now`
- Incluir `days_remaining` para mostrar en UI

## Archivos
- `src/lib/auth/checkPlanAccess.ts` (reescribir)
- `src/app/api/billing/plan/route.ts` (modificar)

## Criterios de aceptación
- [ ] checkPlanAccess con plan Free → solo simulación permitida
- [ ] checkPlanAccess con plan Pro activo → todo permitido
- [ ] checkPlanAccess con plan expirado → no permitido + reason
- [ ] GET /api/billing/plan retorna info sin campos Stripe
- [ ] Incluye days_remaining calculado

## Dependencias
HU-204
